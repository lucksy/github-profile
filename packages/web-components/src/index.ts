// Import the component files to ensure they are registered.
// When this bundle is loaded, the customElements.define() calls within these files will execute.

import './profile-card-wc';
import './single-repo-card-wc';
import './language-usage-card-wc';

// This package doesn't export traditional JS modules/functions for direct use,
// but rather registers custom elements that can be used in HTML.
// If there were any utility functions or enums to export, they could go here.

// console.log('GitHub Profile Web Components registered.');
